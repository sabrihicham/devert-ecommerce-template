import Link from "next/link";
import Image from "next/image";
import { Dumbbell, Headphones, ShieldCheck, Truck } from "lucide-react";
import type { Collection } from "@/lib/db/drizzle/schema";

export const Footer = ({ categories, storeName, logoUrl }: { categories: Collection[]; storeName: string; logoUrl: string | null }) => <footer className="border-t border-border bg-card"><div className="mx-auto grid max-w-7xl gap-10 px-4 py-14 sm:px-6 md:grid-cols-2 lg:grid-cols-4"><div><Link href="/" className="inline-flex items-center gap-2 text-xl font-black">{logoUrl ? <Image src={logoUrl} alt={storeName} width={140} height={48} className="h-12 w-auto object-contain"/> : <><span className="grid size-9 place-items-center rounded-xl bg-primary text-primary-foreground"><Dumbbell className="size-5"/></span>{storeName}</>}</Link><p className="mt-4 max-w-xs text-sm leading-6 text-muted-foreground">مكملات رياضية مختارة لتدعم أداءك، استشفاءك وروتينك اليومي.</p></div><FooterLinks title="الأقسام" links={categories.filter(c => c.isVisible).slice(0,6).map(c => [c.name, `/${c.slug}`])}/><FooterLinks title="خدمة العملاء" links={[["التوصيل والدفع", "/checkout"],["السلة", "/cart"],["طلباتي", "/orders"],["المفضلة", "/wishlist"]]}/><div><h2 className="font-bold">تسوّق بثقة</h2><ul className="mt-4 space-y-3 text-sm text-muted-foreground"><li className="flex items-center gap-2"><Truck className="size-4 text-primary"/>توصيل إلى جميع الولايات</li><li className="flex items-center gap-2"><ShieldCheck className="size-4 text-primary"/>معاملات آمنة</li><li className="flex items-center gap-2"><Headphones className="size-4 text-primary"/>دعم مباشر عند الحاجة</li></ul></div></div><div className="border-t border-border py-5 text-center text-sm text-muted-foreground">© {new Date().getFullYear()} {storeName}. جميع الحقوق محفوظة.</div></footer>;
function FooterLinks({ title, links }: { title: string; links: string[][] }) { return <div><h2 className="font-bold">{title}</h2><ul className="mt-4 space-y-3">{links.map(([label, href]) => <li key={href}><Link href={href} className="text-sm text-muted-foreground transition hover:text-primary">{label}</Link></li>)}</ul></div>; }
